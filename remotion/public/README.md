このディレクトリに置いたファイルは、props の `src` に相対パスで書くと `staticFile()` で解決されます。
例: `public/logo.png` → `"src": "logo.png"`

`fx-sample-logo.png` / `fx-sample-sticker.png` は `examples/fx-overlay.json`
(FxOverlay のサンプル)が使う素材です。外部素材ゼロでレンダリングできるように置いてあります。
