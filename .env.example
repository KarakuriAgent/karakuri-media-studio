HOST=0.0.0.0
PORT=8080
# ComfyUI の models ディレクトリ（不足モデルの自動ダウンロード先。任意）
#COMFY_MODELS_DIR=/path/to/ComfyUI/models
# outputs/ assets/ library/ の置き場（未設定ならリポジトリ直下。NAS などに
# 置きたいときだけ絶対パスを書く。app.db と runtime/ はリポジトリの中のまま）
#KARAKURI_DATA_DIR=/mnt/nas/karakuri-media-studio
